/**
 * This module exports all models required for setting up the database client.
 *
 * It acts as a central point where all database models are collected and exported
 * for configuring the database driver. This ensures that the models are available
 * for initialization and setup when the database client is created.
 *
 * To add new entries, simply export them from the models directory.
 */

export * from '#model/user.model'
