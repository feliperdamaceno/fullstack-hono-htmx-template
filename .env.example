# The host address where the application is running.
# Example: 'localhost' or '127.0.0.1'
HOST=localhost

# The port number where the application will listen.
# Must be between 1 and 65535
PORT=8080

# The URL of the PostgreSQL database.
DATABASE_URL=postgres://user:password@localhost:5432/database

# The secret key used for signing JWT tokens.
# This should be a long, random, and secure string.
JWT_SECRET=Add-Your-Long-And-S@fe-S3cret-Here

# The name of the cookie that stores the authentication token.
# Should be a short and unique string.
JWT_COOKIE_NAME=token

# The duration the auth cookie remains valid.
# Must be a valid time string: e.g., "1d", "2h", "30m"
# This will also be used to set the JWT expiration.
JWT_EXPIRES_IN=30d

# The cost factor for bcrypt hashing.
# Determines the computational complexity of password hashing.
# Must be an integer between 4 and 31.
# Higher values increase security but slow down hashing.
# Recommended: 10-14 for production environments.
BCRYPT_ALGORITHM_COST=12

# The environment the application is running in.
# Must be one of: development, test, qa, staging, production
NODE_ENV=development
